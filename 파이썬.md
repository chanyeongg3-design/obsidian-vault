if와 else
조건문은 공백을 기준으로 구분

year = int(input("태어난 해를 입력하세요."))
gender = (input("성별을 입력하세요"))

if year < 2000 and gender == "남자"  
    result = 1
elif year < 2000 and gender == "여자"
	result = 2
elif year >= 2000 and gender == "남자"
	result = 3
elif year >= 2000 and gender == "여자"
	result = 4 

print(result)

해설:
둘 다 만족할 때 True
하나라도 다르면 False 




## attend, extend, insert
"""

member = ["karina]

extra_member = ["winter", "ningning"]

  

### final_member == ["karina","winter","ningning","Giselle"]

"""

member = ["karina"]

member.extend(["winter","ningning"])

extra_member = member

print("extra_member:", extra_member)

extra_member.extend(["Giselle"])

final_member = extra_member

print("final_member:", final_member)

========================================

### remove, pop, 

titles = ["복습", "제출", "정리"]

titles.remove("제출")

print(titles)

last = titles.pop(0)

print(last)


========================================


"""

numbers = [3, 6, 32, 15, 4, 1]

  
# 숫자 32를 지우고

그 후 index 2번째 element를 지우고

그 후 반대로 정렬하시오

결과를 print 하시오

"""

  

numbers = [3, 6, 32, 15, 4, 1]

  

# 32 지우기

numbers.remove(32)

print(numbers) # [3, 6, 15, 4, 1]

  

# index 2번째 숫자 지우기

numbers.pop(2)

print(numbers) # [3, 6, 4, 1]

  
  

# 반대로 정렬(내림차순)

numbers.sort(reverse=True) # [6, 4, 3, 1]

  

# 결과 출력

print(numbers) # [6, 4, 3, 1]